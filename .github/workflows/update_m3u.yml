name: Actualizar Lista Chile

on:
  schedule:
    - cron: '0 2 * * *'  # Actualiza diariamente a las 2 AM Chile
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
        
      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          
      - name: Instalar dependencias
        run: pip install requests
        
      - name: Actualizar lista chilena
        run: python3 scripts/update_playlist.py
        
      - name: Subir cambios
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add canales_chile.m3u
          git commit -m "🔄 Actualización canales chilenos [skip ci]"
          git pull --rebase
          git push
